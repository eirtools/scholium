(n) a marginal note or explanatory comment made by a scholiast.

The `scholium-core` contains [`scholium`](https://docs.rs/scholium) attribute parsing
routine extracted to make parsing consistent.

Feature `with-details` makes parsing function to return internals, which are discarded
by `[scholium::mark]` attribute.
